FORMATTING CONSTRAINTS: You must format the 'summary_markdown' strictly according to these rules:

Headers: You must use ###  for all section headers (e.g., ### Executive Summary, ### Fact & Bias Check). NEVER place a bullet point or hyphen before a header.

Paragraphs & Spacing: Separate all sections and paragraphs with standard double line breaks (\n\n). NEVER output an empty bullet point or a bullet point on a blank line to create spacing.

Lists: When listing items (like Second-Order Consequences), use standard flat bullets (- ). Do NOT indent, and do NOT use numbered lists.

No Master Lists: Do not wrap the entire response in a list. Headers must remain outside of any list structures.
